---
title: Chip
description: Compact chip component for tags, filters, and selection with removable functionality and multiple variants.
---

import { DynamicChipShowcase } from '@/components';

# Chip

Compact elements that represent inputs, attributes, or actions. Chips allow users to enter information, make selections, filter content, or trigger actions.

## Installation

```bash
npm install @forteui/core
```

## Import

```tsx
import { Chip } from '@forteui/core';
```

## Basic Usage

```tsx
import { Chip } from '@forteui/core';

function MyComponent() {
  return (
    <Chip variant="filled" color="primary" size="medium">
      Default Chip
    </Chip>
  );
}
```

<DynamicChipShowcase />

## Variants

### Filled Chips

```tsx
<div className="flex flex-wrap gap-2">
  <Chip variant="filled" color="default" size="medium">Default</Chip>
  <Chip variant="filled" color="primary" size="medium">Primary</Chip>
  <Chip variant="filled" color="secondary" size="medium">Secondary</Chip>
  <Chip variant="filled" color="success" size="medium">Success</Chip>
  <Chip variant="filled" color="warning" size="medium">Warning</Chip>
  <Chip variant="filled" color="error" size="medium">Error</Chip>
</div>
```

### Outlined Chips

```tsx
<div className="flex flex-wrap gap-2">
  <Chip variant="outlined" color="default" size="medium">Default</Chip>
  <Chip variant="outlined" color="primary" size="medium">Primary</Chip>
  <Chip variant="outlined" color="secondary" size="medium">Secondary</Chip>
  <Chip variant="outlined" color="success" size="medium">Success</Chip>
  <Chip variant="outlined" color="warning" size="medium">Warning</Chip>
  <Chip variant="outlined" color="error" size="medium">Error</Chip>
</div>
```

### Soft Chips

```tsx
<div className="flex flex-wrap gap-2">
  <Chip variant="soft" color="default" size="medium">Default</Chip>
  <Chip variant="soft" color="primary" size="medium">Primary</Chip>
  <Chip variant="soft" color="secondary" size="medium">Secondary</Chip>
  <Chip variant="soft" color="success" size="medium">Success</Chip>
  <Chip variant="soft" color="warning" size="medium">Warning</Chip>
  <Chip variant="soft" color="error" size="medium">Error</Chip>
</div>
```

## Sizes

```tsx
<div className="flex flex-wrap items-center gap-2">
  <Chip variant="filled" color="primary" size="small">Small</Chip>
  <Chip variant="filled" color="primary" size="medium">Medium</Chip>
</div>
```

## With Icons

### Start Icons

```tsx
import { IconUser, IconSettings, IconCheck } from '@forteui/core';

<div className="flex flex-wrap gap-2">
  <Chip 
    variant="filled"
    color="primary"
    size="medium"
    startIcon={<IconUser />}
  >
    User
  </Chip>
  
  <Chip 
    variant="outlined"
    color="secondary"
    size="medium"
    startIcon={<IconSettings />}
  >
    Settings
  </Chip>
  
  <Chip 
    variant="soft"
    color="success"
    size="medium"
    startIcon={<IconCheck />}
  >
    Verified
  </Chip>
</div>
```

### End Icons

```tsx
import { IconChevronDown, IconX, IconArrowRight } from '@forteui/core';

<div className="flex flex-wrap gap-2">
  <Chip 
    variant="filled"
    color="warning"
    size="medium"
    endIcon={<IconChevronDown />}
  >
    Category
  </Chip>
  
  <Chip 
    variant="outlined"
    color="error"
    size="medium"
    endIcon={<IconX />}
  >
    Remove
  </Chip>
  
  <Chip 
    variant="soft"
    color="info"
    size="medium"
    endIcon={<IconArrowRight />}
  >
    Next
  </Chip>
</div>
```

## Removable Chips

### Basic Removable

```tsx
import { useState } from 'react';
import { Chip, IconX } from '@forteui/core';

function RemovableChipsExample() {
  const [chips, setChips] = useState([
    { id: 1, label: 'React' },
    { id: 2, label: 'TypeScript' },
    { id: 3, label: 'Tailwind CSS' },
    { id: 4, label: 'Next.js' }
  ]);
  
  const handleDelete = (chipId: number) => {
    setChips(chips.filter(chip => chip.id !== chipId));
  };
  
  return (
    <div className="flex flex-wrap gap-2">
      {chips.map(chip => (
        <Chip 
          key={chip.id}
          variant="filled"
          color="primary"
          size="medium"
          endIcon={<IconX />}
          onEndIconClick={() => handleDelete(chip.id)}
        >
          {chip.label}
        </Chip>
      ))}
    </div>
  );
}
```

### With Action Icons

```tsx
import { Chip, IconStar, IconX } from '@forteui/core';

<div className="flex flex-wrap gap-2">
  <Chip 
    variant="filled" 
    color="info" 
    size="medium"
    startIcon={<IconStar />}
    endIcon={<IconX />}
    onStartIconClick={() => console.log('Star clicked')}
    onEndIconClick={() => console.log('Remove favorite')}
  >
    Favorite
  </Chip>
</div>
```

## API Reference

### Props

The Chip component accepts the following props:

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'filled' \| 'outlined' \| 'soft'` | `'filled'` | The visual style variant of the chip |
| `color` | `'default' \| 'primary' \| 'secondary' \| 'info' \| 'success' \| 'warning' \| 'error'` | `'default'` | The color theme of the chip |
| `size` | `'small' \| 'medium'` | `'medium'` | The size of the chip |
| `startIcon` | `React.ReactNode` | - | Icon to display at the start (left side) of the chip |
| `endIcon` | `React.ReactNode` | - | Icon to display at the end (right side) of the chip |
| `onStartIconClick` | `() => void` | - | Handler function called when the start icon is clicked |
| `onEndIconClick` | `() => void` | - | Handler function called when the end icon is clicked |
| `children` | `React.ReactNode` | - | The content to display inside the chip |
| `className` | `string` | - | Additional CSS classes to apply |

### Examples

#### Basic Implementation

```tsx
import { Chip } from '@forteui/core';

// Basic chip
<Chip variant="filled" color="primary" size="medium">
  Primary Chip
</Chip>

// With start icon
<Chip 
  variant="outlined" 
  color="success" 
  size="medium"
  startIcon={<IconCheck />}
>
  Completed
</Chip>

// With end icon (removable)
<Chip 
  variant="soft" 
  color="info" 
  size="medium"
  endIcon={<IconX />}
  onEndIconClick={() => console.log('Remove clicked')}
>
  Removable
</Chip>

// With both icons
<Chip 
  variant="filled" 
  color="warning" 
  size="medium"
  startIcon={<IconStar />}
  endIcon={<IconX />}
  onStartIconClick={() => console.log('Star clicked')}
  onEndIconClick={() => console.log('Remove clicked')}
>
  Favorite
</Chip>
```

#### Interactive Example

```tsx
import { useState } from 'react';
import { Chip, IconX } from '@forteui/core';

function InteractiveChips() {
  const [tags, setTags] = useState(['React', 'TypeScript', 'Next.js']);
  
  const removeTag = (tagToRemove: string) => {
    setTags(tags.filter(tag => tag !== tagToRemove));
  };
  
  return (
    <div className="flex flex-wrap gap-2">
      {tags.map(tag => (
        <Chip 
          key={tag}
          variant="filled"
          color="primary"
          size="medium"
          endIcon={<IconX />}
          onEndIconClick={() => removeTag(tag)}
        >
          {tag}
        </Chip>
      ))}
    </div>
  );
}
```

## Design Specifications

The Chip component follows exact design specifications:

### Medium Size (32px height)
- Height: 32px (fixed)
- Outer padding: 4px left/right
- Label padding: 8px left/right
- Border radius: 10px (filled), 8px (outlined/soft)
- Typography: 13px, weight 500, line-height 18px

### Small Size (24px height)
- Height: 24px (fixed)
- Outer padding: 3px left/right
- Label padding: 5px left/right
- Border radius: 8px (all variants)
- Typography: 13px, weight 500, line-height 18px

### Action Icons
- Icon container: 20px × 20px (both sizes)
- Icon size: 16.67px
- Opacity: 48% (default), 75% (hover)
- Start icon margin-right: 4px
- End icon margin-left: 4px

### Color Variants

- **Filled**: Background uses semantic color main, text uses contrast colors
- **Outlined**: 1px solid border with semantic color, transparent background
- **Soft**: 16% alpha semantic color background, darker text variant

## Related Components

- [Button](/docs/components/button) - For primary actions
- [Badge](/docs/components/badge) - For status indicators
- [Input](/docs/components/input) - For tag input fields
